export interface Skill {
  id: number;
  title: string;
  icon?: string;
}

export interface Framework {
  id: number;
  name: string;
  icon?:string;
}

export interface CoreSkill {
  id: number;
  name: string;
  icon?:string;
}